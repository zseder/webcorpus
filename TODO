- dependency checker script (instead of configure)
- split Makefile into local-dependent and local-independent parts
- clean_wire_encoding.py is very slow, maybe should be reimplemented in C/C++
- help text should be added to binaries
- add paper link and bibtex when published
